# Chaser agent MCP Adapter Notes

MCP adapters must distinguish resources, tools, and prompts. Evals should check that tool calls are scoped, outputs are treated as data, and prompt injection does not grant authority.
